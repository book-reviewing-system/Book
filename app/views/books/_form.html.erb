<%= form_with(model: book, local: true) do |form| %>
  <% if book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>
  <div class="field">
  <%= form.label :category %>
  <%= select_tag(:category_id, options_for_select(@categories), :prompt => "Select a category") %></div>

  <div class="field">
    <%= form.label :number_of_pages %>
    <%= form.number_field :number_of_pages %>
  </div>
  <div class="field">
    <%= form.label :author %>
    <%= form.text_field :author %>
  </div>
  <div class="field">
    <%= form.label :published_date %>
    <%= form.date_select :published_date %>
  </div>
<!--   <div class="field">
    <%= form.label :status %>
    <select name="status">
      <option value="0" >available</option>
      <option value="1">non-available</option>
     </select>
   </div> -->
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
