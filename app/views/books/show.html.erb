<% provide(:title, @book.title) %>

<div class="center">
  <h1><%= @book.title %></h1>
  <h4>by <%= @book.author %></h4>
  <h4><%= @book.number_of_pages %> pages</h4>
  <h4>Pulished: <%= @book.published_date.to_date %></h4>
</div>
