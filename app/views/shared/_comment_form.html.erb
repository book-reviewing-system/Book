<li id="comment_form_<%= review.id %>">
  <%= form_for [@book, review, Comment.new], remote: true do |f| %>
    <%= render 'shared/error_mess', object: f.object %>
      <%= f.text_field :content, placeholder: "Comment" %>
    <%= f.submit "Post", class: "btn" %>
  <% end %>
</li>